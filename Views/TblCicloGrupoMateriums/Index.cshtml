@model IEnumerable<ControlEscolar.Models.TblCicloGrupoMaterium>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FidCicloNavigation.NombreCiclo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FidGrupoNavigation.NombreGrupo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FidMateriaNavigation.NombreMateria)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FidCicloNavigation.NombreCiclo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FidGrupoNavigation.NombreGrupo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FidMateriaNavigation.NombreMateria)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdCicloxGrupoxMateria">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdCicloxGrupoxMateria">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdCicloxGrupoxMateria">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
